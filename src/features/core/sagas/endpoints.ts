import axios from 'axios';

const COLLECTIONS_LIMIT = 8;
const ARTWORKS_LIMIT = 4;

// export const API_URL = 'https://staging.marcelforart.com/api'; // prod

// export const getAuth = ({ userId, offset }) => 
//     `${API_URL}/auth/login/`;

// export const getCollections = (userId, offset) =>
//     axios.get(getCollectionsEndpoint({ userId, offset }));

// export const getArtworks = (userId, offset) =>
//     axios.get(getArtworksEndpoint({ userId, offset }));

// export const getSelectedCollection = ({ userId, collectionUrl }) =>
//     axios.get(getSelectedCollectionEndpoint({ userId, collectionUrl }));